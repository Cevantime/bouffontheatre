<h2>Rendez-vous</h2>

<table class="table">
    <tbody>
    <tr>
        <th>Date de début</th>
        <td>{{ booking.beginAt ? booking.beginAt|date('Y-m-d H:i:s') : '' }}</td>
    </tr>
    <tr>
        <th>Date de fin</th>
        <td>{{ booking.endAt ? booking.endAt|date('Y-m-d H:i:s') : '' }}</td>
    </tr>
    <tr>
        <th>Titre</th>
        <td>{{ booking | event_title }}</td>
    </tr>
    <tr>
        <th>Type</th>
        <td>{{ booking.type | trans }}</td>
    </tr>
    </tbody>
</table>

