{% for type in ['success', 'danger', 'warning', 'info'] %}
    {% for msg in app.flashes(type) %}
        <div class="alert alert-{{ type }}">
            {{ msg }}
        </div>
    {% endfor %}
{% endfor %}